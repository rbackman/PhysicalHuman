
#include "depth_image.h"

/*
for (int i=0;i<_image->w();i++)
{
	for(int j=0;j<_image->h();j++)
	{
		if(_image->ptpixel(j,i)->r>0)
			points.push(GsVec((float)i/_image->w(),(float)j/_image->w(),(float)_image->ptpixel(j,i)->r/255.0f));
	}
}

_image = new GsImage;
if(!_image->load(imagename))
{
	phout<<"could not load image "<<imagename<<gsnl;
}
*/